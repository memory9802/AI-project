# 🎯 登入系統完整測試指南

## ✅ 系統已完成設置

### 已完成的步驟：
1. ✅ 安裝所有依賴套件
2. ✅ 創建 5 個測試用戶到資料庫
3. ✅ 啟動 Flask 應用（運行在 http://localhost:9000）

---

## 📝 測試帳號資訊

所有測試帳號的密碼都是：**`password123`**

| 用戶名 | 電子郵件 | 密碼 | 風格偏好 |
|--------|----------|------|----------|
| testuser1 | test1@example.com | password123 | 休閒 |
| testuser2 | test2@example.com | password123 | 正式 |
| testuser3 | test3@example.com | password123 | 運動 |
| testuser4 | test4@example.com | password123 | 街頭 |
| testuser5 | test5@example.com | password123 | 韓風 |

---

## 🧪 測試步驟

### 1️⃣ 測試登入功能

1. **打開登入頁面**
   - 瀏覽器訪問：http://localhost:9000/login
   - 您應該看到一個美觀的登入介面

2. **使用測試帳號登入**
   - 電子郵件：`test1@example.com`
   - 密碼：`password123`
   - 點擊「登入」按鈕

3. **預期結果**
   - ✅ 登入成功後會自動跳轉到首頁 (`/home`)
   - ✅ 瀏覽器會保存 Session，刷新頁面仍保持登入狀態

4. **測試錯誤情況**
   - 輸入錯誤的密碼（如：`wrongpassword`）
   - 預期：顯示紅色錯誤訊息「電子郵件或密碼錯誤」
   - 輸入不存在的電子郵件（如：`notexist@example.com`）
   - 預期：顯示紅色錯誤訊息「電子郵件或密碼錯誤」

---

### 2️⃣ 測試註冊功能

1. **切換到註冊頁面**
   - 點擊「註冊」標籤

2. **填寫註冊表單**
   - 用戶名稱：`newuser`（隨意填寫）
   - 電子郵件：`newuser@example.com`（使用新的電子郵件）
   - 密碼：`password123`
   - 確認密碼：`password123`

3. **點擊註冊按鈕**
   - 預期：註冊成功後自動登入並跳轉到首頁

4. **測試驗證功能**
   - 密碼不一致：輸入不同的密碼和確認密碼
     - 預期：顯示錯誤「兩次輸入的密碼不一致」
   - 密碼太短：輸入少於 6 個字元的密碼
     - 預期：顯示錯誤「密碼長度至少需要 6 個字元」
   - 重複的電子郵件：使用已存在的郵箱註冊
     - 預期：顯示錯誤「此電子郵件已被註冊」

---

### 3️⃣ 測試密碼可見性切換

1. 在密碼輸入框右側有一個「眼睛」圖標
2. 點擊圖標可以切換密碼的可見/隱藏狀態
3. 測試登入和註冊表單的所有密碼欄位

---

### 4️⃣ 使用 API 測試（進階）

#### 測試登入 API

```bash
curl -X POST http://localhost:9000/api/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test1@example.com",
    "password": "password123"
  }'
```

**預期回應：**
```json
{
  "success": true,
  "message": "登入成功",
  "user": {
    "id": 1,
    "username": "testuser1",
    "email": "test1@example.com",
    "favorite_style": "休閒"
  }
}
```

#### 測試註冊 API

```bash
curl -X POST http://localhost:9000/api/register \
  -H "Content-Type: application/json" \
  -d '{
    "username": "apitest",
    "email": "apitest@example.com",
    "password": "password123"
  }'
```

**預期回應：**
```json
{
  "success": true,
  "message": "註冊成功",
  "user": {
    "id": 6,
    "username": "apitest",
    "email": "apitest@example.com",
    "favorite_style": "休閒"
  }
}
```

#### 測試登出 API

```bash
curl -X POST http://localhost:9000/api/logout \
  -H "Content-Type: application/json" \
  -c cookies.txt -b cookies.txt
```

**預期回應：**
```json
{
  "success": true,
  "message": "已成功登出"
}
```

#### 測試取得用戶資訊 API（需先登入）

```bash
# 先登入並保存 cookie
curl -X POST http://localhost:9000/api/login \
  -H "Content-Type: application/json" \
  -d '{"email":"test1@example.com","password":"password123"}' \
  -c cookies.txt

# 使用 cookie 取得用戶資訊
curl -X GET http://localhost:9000/api/user \
  -b cookies.txt
```

**預期回應：**
```json
{
  "success": true,
  "user": {
    "id": 1,
    "username": "testuser1",
    "email": "test1@example.com",
    "favorite_style": "休閒"
  }
}
```

---

## 🛠️ 實用命令

### 查看應用運行狀態

```bash
# 查看 Flask 應用進程
ps aux | grep "python app.py"

# 查看端口占用
lsof -i :9000
```

### 停止應用

```bash
# 找到進程 ID 並停止
lsof -ti:9000 | xargs kill -9
```

### 重啟應用

```bash
cd /Users/wenyinkai/Downloads/AI-project-1202MVP/app
PORT=9000 python app.py
```

### 查看數據庫中的用戶

```bash
# 使用 Docker 連接 MySQL
docker exec outfit-mysql mysql -uroot -prootpassword outfit_db \
  -e "SELECT id, username, email, favorite_style FROM users;"
```

---

## 🐛 常見問題排解

### 問題 1：無法訪問登入頁面

**解決方法：**
1. 確認 Flask 應用正在運行
   ```bash
   ps aux | grep "python app.py"
   ```
2. 確認端口 9000 沒有被其他程序占用
   ```bash
   lsof -i :9000
   ```
3. 重新啟動應用

### 問題 2：登入後沒有跳轉

**可能原因：**
- 前端 JavaScript 錯誤
- Session 配置問題

**解決方法：**
1. 打開瀏覽器開發者工具（F12）
2. 查看 Console 是否有 JavaScript 錯誤
3. 查看 Network 標籤，確認 API 請求是否成功

### 問題 3：密碼驗證失敗

**確認事項：**
1. 密碼是否正確（測試帳號密碼統一為 `password123`）
2. 檢查數據庫中的密碼 hash 是否正確
   ```bash
   docker exec outfit-mysql mysql -uroot -prootpassword outfit_db \
     -e "SELECT username, email, password_hash FROM users WHERE email='test1@example.com';"
   ```

### 問題 4：資料庫連接失敗

**確認事項：**
1. MySQL Docker 容器是否運行
   ```bash
   docker ps | grep mysql
   ```
2. 環境變數是否正確配置
   - `DB_HOST=localhost`
   - `DB_PORT=3306`
   - `DB_USER=root`
   - `DB_PASS=rootpassword`
   - `DB_NAME=outfit_db`

---

## 📊 測試檢查清單

使用以下清單確保所有功能正常：

- [ ] ✅ 使用正確帳號密碼登入成功
- [ ] ✅ 輸入錯誤密碼顯示錯誤訊息
- [ ] ✅ 輸入不存在的電子郵件顯示錯誤訊息
- [ ] ✅ 註冊新用戶成功
- [ ] ✅ 密碼不一致時顯示錯誤
- [ ] ✅ 密碼太短時顯示錯誤
- [ ] ✅ 使用已存在的郵箱註冊顯示錯誤
- [ ] ✅ 密碼可見性切換功能正常
- [ ] ✅ 登入後自動跳轉到首頁
- [ ] ✅ 登出功能正常
- [ ] ✅ API 端點回應正確的 JSON
- [ ] ✅ Session 在頁面刷新後保持登入狀態

---

## 🎉 測試成功標準

當以下所有項目都通過時，登入系統測試完成：

1. ✅ 能使用 5 個測試帳號中的任意一個成功登入
2. ✅ 能成功註冊新用戶
3. ✅ 所有錯誤情況都能正確顯示錯誤訊息
4. ✅ API 回應格式正確且數據完整
5. ✅ Session 管理正常工作

---

## 📞 需要幫助？

如果遇到任何問題，請檢查：

1. **應用日誌**：查看終端的應用輸出
2. **瀏覽器控制台**：按 F12 打開開發者工具
3. **數據庫狀態**：確認 MySQL 容器正常運行
4. **網絡請求**：使用瀏覽器 Network 標籤查看 API 請求

---

## 🚀 下一步

測試完成後，您可以：

1. 整合登入系統到其他頁面（wardrobe、share、recommendation）
2. 添加密碼重設功能
3. 實現 OAuth 第三方登入（Google、Facebook）
4. 添加用戶個人資料編輯功能
5. 實現記住我功能

享受測試！ 🎊
