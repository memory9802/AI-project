# 🎯 AI 穿搭推薦專案 - 規格化開發指南

> **專案 OpenSpec 規格化開發完整指南**  
> 適用於 5 人協作團隊的標準化開發環境與工作流程

---

## 📑 目錄

1. [🚀 快速開始](#快速開始)
2. [🏗️ 規格化架構](#規格化架構)
3. [⚙️ 環境設置](#環境設置)
4. [🔧 配置管理](#配置管理)
5. [👥 團隊協作流程](#團隊協作流程)
6. [📋 OpenSpec 工作流程](#openspec-工作流程)
7. [🐛 問題排除](#問題排除)
8. [📚 附錄與參考](#附錄與參考)

---

## 🚀 快速開始

### 🎯 我該看哪個章節？

| 角色/情境 | 建議閱讀 | 預計時間 |
|----------|----------|----------|
| 🆕 新組員加入 | [環境設置](#環境設置) → [團隊協作流程](#團隊協作流程) | 30-45 分鐘 |
| 🔧 環境問題 | [配置管理](#配置管理) → [問題排除](#問題排除) | 15-30 分鐘 |
| 📝 功能開發 | [OpenSpec 工作流程](#openspec-工作流程) → [團隊協作流程](#團隊協作流程) | 20-30 分鐘 |
| 🚨 緊急修復 | [問題排除](#問題排除) | 5-15 分鐘 |

### ⚡ 一分鐘啟動

```bash
# 1. 克隆專案並切換分支
git clone https://github.com/memory9802/AI-project.git
cd AI-project && git checkout system

# 2. 環境檢查（重要！）
./scripts/check-environment.sh  # macOS/Linux
# 或 .\scripts\check-environment.ps1  # Windows

# 3. 設置環境變數
cp .env.example .env
# 編輯 .env 填入實際 API Key

# 4. 啟動服務
docker-compose up --build
```

**✅ 成功指標**：
- 網站可訪問：http://localhost:5001
- 資料庫可管理：http://localhost:8080

---

## 🏗️ 規格化架構

### 🎯 設計原則

我們採用 **OpenSpec 規格驅動開發**，解決團隊協作中的核心問題：

| 傳統問題 | OpenSpec 解決方案 |
|----------|-------------------|
| 🔥 組員修改核心配置檔案 | 配置分離架構：核心 + 個人適配 |
| 🔄 環境不一致導致衝突 | 統一底層架構 + 標準化工具鏈 |
| 📁 個人配置意外提交 | 強化 Git 忽略策略 + 自動檢查 |
| 🤔 需求理解不一致 | 規格先行，再實作 |

### 🔧 核心架構組件

```
AI-project/ (規格化後)
├── 📋 規格與文檔
│   ├── README.md                    # 專案主要說明
│   ├── SPEC_GUIDE.md               # 本檔案：統一指南
│   └── openspec/                   # OpenSpec 規格目錄
│       ├── project.md              # 專案技術規格
│       ├── specs/                  # 正式規格文件
│       └── changes/                # 變更提案
│
├── ⚙️ 配置管理（分離架構）
│   ├── docker-compose.yml          # 🔒 核心配置（禁止修改）
│   ├── docker-compose.override.yml.example  # 個人適配範本
│   ├── .env.example               # 環境變數範本
│   ├── .gitignore                 # 強化忽略規則
│   └── .vscode/                   # 團隊統一開發設定
│
├── 🛠️ 自動化工具
│   ├── scripts/check-environment.sh    # 環境檢查腳本
│   └── scripts/check-environment.ps1   # Windows 版本
│
└── 📱 應用程式
    ├── app/                       # Flask 應用
    ├── init/                      # 資料庫初始化
    └── package.json              # 前端依賴
```

---

## ⚙️ 環境設置

### 📋 前置需求檢查清單

**必備工具：**
- [ ] ✅ Docker Desktop (正在運行)
- [ ] ✅ Node.js v20+ 和 npm
- [ ] ✅ Python 3.12+
- [ ] ✅ Git (已配置用戶資訊)

**環境準備：**
- [ ] ✅ Google Gemini API Key ([申請網址](https://aistudio.google.com/))
- [ ] ✅ 端口 5001、3306、8080 未被佔用
- [ ] ✅ 至少 4GB 可用記憶體

### 🔍 自動環境檢查

```bash
# 執行環境檢查腳本
./scripts/check-environment.sh

# 輸出範例：
# ✅ Docker: 正在運行
# ✅ Python 版本: 3.12.0 (滿足最低需求)
# ⚠️  端口 5001: 被其他服務佔用
# 💡 建議：參考個人配置適配章節
```

### 🎨 VS Code 自動配置

專案包含統一的 VS Code 設定，開啟專案後自動生效：

**自動套用設定：**
- 📝 Python 格式化：Black (line-length: 88)
- 🔧 JavaScript 格式化：Prettier
- 🐛 除錯配置：Flask + Docker 支援  
- 📦 擴充套件建議：Python、Docker、YAML 等

**自訂設定位置：**
- 團隊設定：`.vscode/settings.json` ✅ 可提交
- 個人設定：`.vscode/settings.local.json` ❌ 自動忽略

---

## 🔧 配置管理

### 🎯 配置分離原則

我們採用 **三層配置架構**：

| 配置層級 | 檔案 | 用途 | Git 管理 |
|----------|------|------|----------|
| 🔒 **核心配置** | `docker-compose.yml` | 團隊統一架構 | ✅ 提交 |
| ⚙️ **個人適配** | `docker-compose.override.yml` | 端口、路徑調整 | ❌ 忽略 |
| 🔑 **機密資訊** | `.env` | API Keys | ❌ 忽略 |

### 📝 個人環境適配指南

#### 1. 端口衝突解決

```bash
# 創建個人配置檔案
cp docker-compose.override.yml.example docker-compose.override.yml

# 編輯個人配置（範例）
# services:
#   mysql:
#     ports:
#       - "3307:3306"  # 改用 3307 端口
#   flask:  
#     ports:
#       - "5002:5000"  # 改用 5002 端口
```

#### 2. 環境變數管理

```bash
# 複製環境變數範本
cp .env.example .env

# 填入實際配置
LLM_API_KEY=your_actual_gemini_api_key_here
```

#### 3. 開發工具個人化

```json
// .vscode/settings.local.json (不會被提交)
{
  "editor.fontSize": 14,
  "workbench.colorTheme": "your-preferred-theme"
}
```

### 🛡️ Git 保護策略

**增強版 `.gitignore`** 防止意外提交：

```bash
# 環境變數保護
.env
.env.local
.env.*.local

# Docker 個人配置保護
docker-compose.override.yml
docker-compose.*.yml
!docker-compose.override.yml.example

# VS Code 個人設定保護
.vscode/settings.local.json
.vscode/*.personal.json

# 個人暫存檔案保護
*.tmp
*.bak
personal-notes.md
```

---

## 👥 團隊協作流程

### 🔄 標準開發流程

```bash
# 1️⃣ 同步最新代碼
git pull origin system

# 2️⃣ 環境檢查
./scripts/check-environment.sh

# 3️⃣ 創建功能分支
git checkout -b feature/your-feature-name

# 4️⃣ 開發並測試
docker-compose up -d
# 進行開發工作...

# 5️⃣ 提交變更（只提交核心代碼）
git add .
git commit -m "feat: 新增功能描述"

# 6️⃣ 推送並創建 Pull Request
git push origin feature/your-feature-name
```

### 📋 程式碼提交規範

**Commit Message 格式：**
```
<類型>(<範圍>): <簡短描述>

<詳細說明>（可選）
```

**類型範例：**
- `feat`: 新功能
- `fix`: 修復 bug  
- `docs`: 文檔更新
- `style`: 程式碼格式化
- `refactor`: 重構
- `test`: 測試相關

**範例：**
```bash
git commit -m "feat(ai): 新增服裝推薦演算法

- 實作基於用戶偏好的推薦邏輯
- 新增推薦結果快取機制
- 支援季節性推薦過濾"
```

### ⚠️ 提交前檢查清單

- [ ] 🔍 執行 `./scripts/check-environment.sh` 通過
- [ ] 🧪 本機測試所有功能正常  
- [ ] 📝 Commit message 符合規範
- [ ] 🚫 未包含個人配置檔案（`.env`、`docker-compose.override.yml` 等）
- [ ] ✅ 程式碼通過格式化檢查

---

## 📋 OpenSpec 工作流程

### 🎯 規格驅動開發原則

**先規格，後實作**：每個新功能都需要先創建 OpenSpec 變更提案。

```
需求分析 → 創建提案 → 團隊審查 → 實作開發 → 驗證測試 → 歸檔規格
```

### 📝 創建變更提案

```bash
# 1. 檢查現有規格和變更
openspec list --specs
openspec list

# 2. 搜尋相關知識
openspec search "相關功能關鍵字"

# 3. 創建變更目錄
mkdir -p openspec/changes/add-new-feature/specs

# 4. 編寫提案檔案
# - proposal.md: 變更說明和影響
# - tasks.md: 實作任務清單  
# - specs/: 規格變更內容

# 5. 驗證格式
openspec validate add-new-feature --strict
```

### 📄 提案檔案範本

**proposal.md:**
```markdown
# Change: 功能名稱

## Why
為什麼需要這個變更？

## What Changes  
- 具體變更內容
- **BREAKING**: 標記破壞性變更

## Impact
- 受影響的規格：功能模組清單
- 受影響的程式碼：檔案路徑清單
```

**tasks.md:**
```markdown
## 1. 實作階段
- [ ] 1.1 建立資料模型
- [ ] 1.2 實作 API 端點
- [ ] 1.3 前端介面開發
- [ ] 1.4 編寫測試
```

### 🔄 變更生命週期

1. **📋 提案階段** (`changes/`)
   - 創建變更提案
   - 團隊審查討論
   - 修改直到達成共識

2. **⚡ 實作階段**
   - 按照 tasks.md 執行開發
   - 逐項完成並標記 `- [x]`

3. **📦 歸檔階段**
   - 變更完成後歸檔：`openspec archive change-name`
   - 更新正式規格：`specs/`

### 🤖 與 AI 協作

GitHub Copilot 會自動讀取 `openspec/AGENTS.md` 並理解我們的工作流程：

```bash
# AI 助手可以幫助：
# "請創建 AI 對話系統的 OpenSpec 變更提案"
# "幫我實作 add-recommendation-engine 的任務清單"
# "檢查當前變更提案的完整性"
```

---

## 🐛 問題排除

### 🚨 常見問題快速修復

#### Q1: Docker 容器啟動失敗

```bash
# 檢查 Docker 狀態
docker --version
docker info

# 清理並重建
docker-compose down -v
docker system prune -f  
docker-compose up --build
```

#### Q2: 端口被佔用

```bash
# 檢查端口佔用 (macOS/Linux)
lsof -i :5001 -i :3306 -i :8080

# 檢查端口佔用 (Windows) 
netstat -ano | findstr "5001 3306 8080"

# 解決方案：使用個人配置適配
cp docker-compose.override.yml.example docker-compose.override.yml
# 編輯檔案，修改端口映射
```

#### Q3: API Key 錯誤

```bash
# 檢查環境變數
cat .env | grep LLM_API_KEY

# 重新設置 API Key
# 1. 訪問 https://aistudio.google.com/
# 2. 重新生成 API Key
# 3. 更新 .env 檔案
# 4. 重啟服務：docker-compose restart flask
```

#### Q4: Git 提交被拒絕

```bash
# 檢查是否包含不該提交的檔案
git status
git diff --cached

# 移除意外加入的檔案
git reset HEAD .env
git reset HEAD docker-compose.override.yml

# 更新 .gitignore 並重新提交
git add .gitignore
git commit -m "fix: 更新 gitignore 規則"
```

### 🔍 診斷工具

```bash
# 環境全面檢查
./scripts/check-environment.sh

# OpenSpec 狀態檢查  
openspec list
openspec validate --strict

# Docker 服務檢查
docker-compose ps
docker-compose logs
```

### 📞 獲得幫助

1. **🔍 自助診斷**：執行環境檢查腳本
2. **📖 文檔查詢**：搜尋本指南相關章節  
3. **👥 團隊討論**：在團隊群組發問
4. **🐛 問題回報**：GitHub Issues

---

## 📚 附錄與參考

### 🔗 重要連結

- **GitHub 倉庫**: https://github.com/memory9802/AI-project
- **OpenSpec 官方文檔**: https://openspec.dev/
- **Google Gemini API**: https://aistudio.google.com/
- **Docker 官方文檔**: https://docs.docker.com/

### 📊 專案技術規格

| 組件 | 版本 | 用途 |
|------|------|------|
| Python | 3.12+ | 後端開發 |
| Flask | 3.0.0 | Web 框架 |
| MySQL | 8.0 | 資料庫 |
| Docker | 最新版 | 容器化 |
| Node.js | v20+ | 前端建構 |
| LangChain | 最新版 | AI 整合 |

### 🎯 效能指標

- **啟動時間**: < 30 秒（首次建構）
- **響應時間**: < 2 秒（AI 對話）
- **記憶體使用**: < 2GB（開發環境）
- **並發支援**: 10+ 使用者

### 📋 檢查清單範本

**新功能開發檢查清單：**
- [ ] OpenSpec 變更提案已創建並審查
- [ ] 本機環境檢查通過
- [ ] 功能實作完成並測試
- [ ] 程式碼符合團隊規範
- [ ] 文檔已更新
- [ ] 無個人配置檔案被提交

**問題排除檢查清單：**
- [ ] 環境檢查腳本執行
- [ ] Docker 服務正常運行
- [ ] 端口無衝突
- [ ] API Key 正確配置
- [ ] Git 狀態乾淨

---

## 🎉 總結

通過本規格化指南，我們實現了：

✅ **環境一致性**：所有組員使用相同的底層架構  
✅ **配置分離**：核心穩定，個人靈活適配  
✅ **流程標準化**：OpenSpec 規格驅動開發  
✅ **自動化工具**：環境檢查和問題診斷  
✅ **團隊協作**：清晰的工作流程和規範  

**下一步行動：**
1. 🧪 所有組員按照指南設置環境
2. 📝 創建第一個 OpenSpec 功能提案  
3. 🔄 建立定期的環境同步檢查
4. 📈 持續優化和改進流程

**記住**：規格化開發的核心是 **先規格，後實作** 🎯

---

*最後更新：2025年11月20日*  
*維護團隊：AI-project 5人開發團隊* 🚀